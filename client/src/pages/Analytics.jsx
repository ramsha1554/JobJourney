import React from 'react';
import Dashboard from './Dashboard';

const Analytics = () => {
    // Reuse the Dashboard analytics UI for the dedicated Analytics page
    return <Dashboard />;
};

export default Analytics;
