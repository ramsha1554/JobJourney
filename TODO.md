# TODO

- [ ] Inspect `client/src/pages/LandingPage.jsx` CTA section to ensure we only modify that section.
- [ ] Replace CTA content with new two-column layout and required inline SVG (viewBox 0 0 400 300; width 100%, max-width 420px).
- [ ] Ensure buttons keep existing `onClick`/navigation logic.
- [ ] Add responsive behavior: stack vertically under 768px.
- [ ] Run `cd client && npm run build` and confirm 0 errors.
